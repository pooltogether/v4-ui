import React from 'react'

import Layout from '@src/components/Layout'
import { AccountUI } from '@src/views/Account'

export default function IndexPage(props) {
  return (
    <Layout>
      <AccountUI />
    </Layout>
  )
}
